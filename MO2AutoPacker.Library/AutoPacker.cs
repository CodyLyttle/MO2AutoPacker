namespace MO2AutoPacker.Library;

/*
 * Read checked mods from modlist.txt
 * Warn if large amount of files are unchecked (is user trying to repack the BSA profile)
 * 
 */

public class AutoPacker
{
    public AutoPacker(IModReader reader, string rootPath)
    {
        
    }
}